export default interface ITheme {
        colors: {
            primary: string,
            secondary: string,
            disabled: string,
            card: string,
            border: string
        },
        fonts: {
            title: number,
            subtitle: number,
            text: number,
            caption: number
        }
}